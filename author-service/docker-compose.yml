services:
  api:
    container_name: lms-author-service
    build:
      context: .
      dockerfile: Dockerfile
    image: lms-mini/author-service:v1
    restart: always
    ports:
      - 8082:8082
    volumes:
      - ./logs:/app/logs
